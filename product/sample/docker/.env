# 
# .env 파일이란 docker-compose의 환경변수 정보들을 따로 저장하는 파일이다.
# 1. docker-compose.yml이 위치한 동일 경로에 존재해야한다.
# 2. 평문 텍스트 포맷으로, 아래의 문법을 따라 작성해야한다.
#  1) 각 줄마다 '변수명=값' 형태로 입력한다.
#  2) 주석 처리는 # 문자를 이용한다.
#  3) 비어있는 줄은 무시된다.
#  4) 따옴표 처리는 불필요하다. 입력된 따옴표는 위치에 따라 변수명이나 값의 일부분으로 간주된다.
# 3. 각 서비스마다 .env 파일이 따로 존재할 수 있지만, 같은 변수명이 존재할 경우 나중에 삽입된 파일의 값이 적용된다.
#
COMPOSE_PROJECT_NAME=sample
DOCKER_NAME=sample
VERSION=1.0.0.4
APP_PORT=5000

# DB 정보
DB_IP=sample-db
DB_PORT=5432
DB_USER=sample
DB_PWD=sample
DB_NAME=sample
DB_URL=postgresql://{0}:{1}@{2}:{3}/{4}

# DB Connection pool
SQLALCHEMY_POOL_SIZE=10
SQLALCHEMY_MAX_OVERFLOW=100
SQLALCHEMY_POOL_TIMEOUT=100

SQLALCHEMY_POOL_RECYCLE=300
SQLALCHEMY_POOL_PRE_PING= True
SQLALCHEMY_ECHO=True